/*
The query package includes Cosmos and IstChain gRPC query clients.

To ensure that the `QueryClient` stays updated, add new module query clients
to the `QueryClient` whenever new modules with grpc queries are added to the IstChain app.
*/
package query
